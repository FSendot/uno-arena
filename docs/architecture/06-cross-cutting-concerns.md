# 06 Cross-Cutting Concerns

## Security

- Use the external IdP for user authentication.
- Use provider-neutral OIDC validation inside Identity; the BFF and downstream contexts never parse or trust provider claims directly.
- Allow Keycloak test provisioning/direct credential flows only in explicitly non-production environments; production readiness rejects those flags and insecure issuer/TLS/JWKS configuration.
- Enforce authorization in Identity and the BFF before command dispatch.
- Scope service credentials per bounded context.
- Keep internal audit and replay APIs private.
- Envelope-encrypt Game Integrity seeds, deck order, private card material, reservations, and replay-sensitive payloads before storage; mesh mTLS and disk encryption do not replace this control.
- Grant KMS/HSM decrypt capability only to scoped Game Integrity replay/audit identities, with break-glass authorization and durable decrypt/export audit records.
- Treat spectator traffic as untrusted by default.
- Admit spectators only while a room is `waiting`, `locked`, or `in_progress` subject to public/private authorization; deny new connections and close existing streams after `RoomCompleted` or `RoomCancelled`.
- Keep microservices off the public internet; the BFF is the only external surface.
- Enroll production application, worker, CDC, datastore, and observability namespaces in Istio Ambient; require strict mTLS and dedicated Kubernetes service accounts with default-deny L4 authorization. Local `kind` enrolls `uno-arena` and `observability` to verify the same security path.
- Treat the stable Room router as an application authorization boundary: route-specific caller credentials and BFF-derived player principals are validated before pod lookup, then replaced by a scoped router credential plus generation only for the internal runtime hop.
- Room PgBouncer additionally requires SCRAM-SHA-256 plus TLS on client and server database legs in staging/production. Helm rejects a staging/production topology that omits those controls. The explicit local kind exception applies only to those application-layer database credentials/TLS settings; Istio Ambient strict mTLS still protects its east-west transport.
- Use the repo-owned simple CLI as the sole client/test interface for this implementation; deferred graphical clients must preserve the BFF-only REST/SSE boundary.

## Privacy

- Spectator View owns the privacy filter.
- Private hands, hidden deck state, and player-only decisions never cross into spectator projections.
- Public analytics must stay anonymized or explicitly public.
- Sanitized events are better than ad-hoc client filtering.

## Threat Model

Primary threats:

- stale or replayed commands
- session hijacking or displaced sessions
- spectator data leakage
- duplicate event delivery
- timer manipulation or replay
- cross-service contract drift

Mitigations:

- `commandId` idempotency keys
- `expectedSequenceNumber` checks
- `SessionInvalidated` stream closure
- authoritative Identity Postgres validation for each state-changing command; no cached-positive authorization fallback
- privacy-filtered projections
- schema versioning on events
- durable timer state outside Redis, with absolute UTC `expiresAt` and opening room sequence as server authority
- structured operational/security audit records for rejected commands
- spectator admission limited to non-terminal room states, with terminal stream closure
- bounded-context-specific Kafka topics
- least-privilege PostgreSQL logical-replication users and isolated Debezium connector credentials

## Observability

- Centralize Kubernetes workload logs through Grafana Alloy into Grafana Loki; Grafana provides the query and visualization surface.
- Retain Loki WAL disk backpressure at `0.90` in production and `0.99` in the disposable kind overlay. The kind node shares the local container engine's image filesystem and may start above 90 percent while retaining several GiB for the bounded acceptance run; local disk protection remains enabled.
- Loki uses TSDB with an S3-compatible object-store contract in every environment. Its monolithic ingester enables the WAL, explicitly configures the TSDB shipper's active-index and query-cache directories, and flushes on graceful shutdown. Persistence acceptance creates a fresh player exactly once through the public CLI with a dedicated low-volume correlation marker; it never replays an unknown registration, but may safely repeat an authenticated `whoami` read with the same marker until the recovered log path is observed. It then invokes the supported shutdown with flushing and full-process termination so Loki cuts the active 15-minute TSDB head and the shipper performs its final synchronous upload. After kubelet restarts that same Pod, acceptance deletes the exact Pod identity to remove its local WAL/index and requires the distinct replacement to query the correlated log from S3 before replacing MinIO. Tempo independently preserves the canonical cross-context gameplay trace. Local `kind` deploys MinIO with separate `loki-logs` and `tempo-traces` buckets on one 5 GiB PVC that survives MinIO pod replacement but is intentionally destroyed with the cluster. Loki's Compactor and Tempo independently enforce 24-hour retention for their buckets; local MinIO has no competing lifecycle deletion rule. Production defaults to external S3 buckets with environment-owned endpoint mode, bucket, region, TLS, credentials, encryption, and lifecycle policy.
- Alloy is a collector and forwarder, not the durable log store. Filesystem-only Loki storage and Alloy-only retention are not supported deployment paths.
- Use the OpenTelemetry Go Meter API for repository-owned application and business metrics. Each process exposes a service-owned `/metrics` endpoint through the OpenTelemetry Prometheus exporter on named internal port `metrics` (`9090`); standalone Prometheus scrapes those endpoints directly. Use an explicit underscore-and-type-suffix translation, a process-local custom Prometheus registry, and native Go/process collectors. Prometheus uses EndpointSlice discovery for stable API pods through their existing Kubernetes Services, whose `metrics` port targets the named container port. Worker and dedicated Room runtime pods do not gain metrics-only Services; Prometheus discovers them directly by their named port and explicit pod scrape label, including unready targets for failure diagnosis. Service-discovered and pod-discovered workloads carry mutually exclusive scrape modes, and validation rejects duplicate or unclassified application targets. Local scrape and evaluation intervals are 15 seconds with a 5-second scrape timeout; acceptance polls for baseline and post-flow observations for up to 45 seconds so scheduler timing cannot make the proof flaky. Production may override the cadence. The metric path is pull-based and does not pass through Alloy. Grafana is provisioned with both Prometheus and Loki data sources. The local acceptance lane does not require Mimir, Prometheus Operator, Alertmanager, or the full `kube-prometheus-stack`.
- Local Prometheus stores its TSDB on a 5 GiB PVC, retains 24 hours of history, and applies a size ceiling below filesystem capacity for headroom. History survives Prometheus pod replacement but is intentionally deleted with the kind cluster; local metrics make no backup, cross-cluster durability, HA, RPO, or RTO claim. Production retention and durable metrics storage remain environment-owned.
- Grafana is a stateless, private operator surface. Prometheus, Loki, and Tempo data sources plus the business and cluster dashboards are provisioned from version-controlled ConfigMaps and marked non-editable; a replacement pod reconstructs them without a Grafana PVC. Optional default plugin preinstallation is disabled so deployment never depends on runtime catalog downloads; the provisioned dashboards use built-in panels and data sources. Local admin credentials come from the local Kubernetes Secret. Grafana remains ClusterIP-only and local access binds a `kubectl port-forward` to `127.0.0.1`; acceptance validates both dashboards through the Grafana HTTP API without manual UI setup.
- Run Alloy, Loki, Tempo, MinIO, Prometheus, kube-state-metrics, and Grafana in a dedicated `observability` namespace, separate from `uno-arena`. Use separate `alloy-logs` and `alloy-otlp` service accounts: the DaemonSet receives only read-only pod metadata access and a read-only `/var/log/pods` host mount, while the gateway disables token automount and has no Kubernetes API permissions. Because container runtimes expose that node log directory as `0750 root:root`, only the non-root node-log collector receives supplemental GID 0 to traverse and read the mount; it keeps `runAsNonRoot`, `RuntimeDefault` seccomp, a read-only root filesystem, disabled privilege escalation, and all Linux capabilities dropped. Prometheus receives only read-only discovery plus scoped kubelet/cAdvisor proxy access; kube-state-metrics receives only the required list/watch verbs for pods and deployments. Loki, Tempo, MinIO, and Grafana disable service-account token automount and receive no Kubernetes API access. No observability identity may read application Secrets. Enroll both namespaces in Istio Ambient. Application `/metrics` endpoints stay internal; strict mTLS authenticates the scrape, an L4 authorization policy permits only the Prometheus service-account principal on port `9090`, and production Kubernetes NetworkPolicy adds the same restriction where enforced. The private Alloy gateway Service exposes OTLP/gRPC `4317` and OTLP/HTTP `4318`; applications default to gRPC. Default-deny mesh authorization allows those ports only from dedicated application service accounts in `uno-arena`, with production NetworkPolicy defense in depth and no bearer/shared telemetry secret. Each production ingress policy admits the Ambient HBONE transport on `15008`, while Istio continues authorizing the original port and SPIFFE principal; link-local Ambient kubelet probe addresses are restricted to the workload health port. The kind acceptance lane uses strict Ambient mTLS and the same identity-scoped Istio authorization, while production renders enable the CNI-enforced NetworkPolicies. Every observability backend ingress is separately identity- and port-scoped: Alloy Logs/Grafana to Loki, Alloy OTLP/Grafana to Tempo, Grafana to Prometheus, Prometheus to kube-state-metrics, and Loki/Tempo/bucket bootstrap to MinIO. Applications never ingest directly into Loki or Tempo.
- Repository-owned Go services and workers emit one valid JSON object per line through `log/slog`. Required base fields are UTC RFC3339Nano `timestamp`, lowercase `level`, `service`, `component`, `environment`, `version`, `instance_id`, and a bounded snake-case `event`; `message` is optional. Valid active spans add `trace_id` and `span_id`. Domain/correlation fields keep canonical contract spelling such as `correlationId`, `commandId`, `eventId`, `roomId`, `gameId`, and `tournamentId`. Sanitized failures use only `error`, not `err`; source paths and stack dumps are omitted; callers cannot override reserved base fields. Startup failures log structurally before exit rather than using `log.Fatal` paths that bypass bounded telemetry shutdown. Correlation and domain identifiers are queryable parsed fields but never Loki labels. Alloy collects pods only from the configured application namespace, including its third-party foundation workloads and dynamically created Room runtimes; excluding the observability and control-plane namespaces prevents collector/backend logs from feeding back into Loki. Alloy labels only bounded namespace, service, component, container, and level dimensions, explicitly dropping automatic filename and CRI stream labels; Loki disables duplicate automatic `service_name` discovery. Pod names and command, event, correlation, player, room, game, and tournament identifiers remain structured metadata. Third-party infrastructure logs in the application namespace may remain raw with the same bounded Kubernetes labels.
- Never log credentials, bearer tokens, passwords, private hands, deck order or seed material, private card identities, complete event/request payloads, encryption material, or connection strings. Error fields carry sanitized messages rather than dumped objects.
- Instrument repository-owned Go services and workers with OpenTelemetry. Propagate W3C Trace Context across HTTP and Kafka headers, and export OTLP/gRPC or OTLP/HTTP to a dedicated Alloy gateway Deployment. Alloy batches/processes traces and forwards them to monolithic Tempo in local `kind`; no separate OpenTelemetry Collector is deployed.
- Instrument adapter construction with established libraries: `otelhttp` for inbound/outbound HTTP, franz-go `kotel` for direct Kafka clients, `redisotel` tracing for Redis clients, and `otelpgx` for `pgxpool`; wrap Analytics' existing ClickHouse HTTP transport with `otelhttp`. Game Integrity adds manual context-owned spans around KurrentDB append/read/revision operations instead of exposing opaque internal gRPC spans. Bounded contexts manually define command, durable-commit, publication, and worker spans. Operation and route names are bounded, and instrumentation never records SQL text, Redis keys, raw payloads, credentials, or domain identifiers as metric labels.
- Use one OpenTelemetry resource identity contract across logs, metrics, and traces: stable canonical `service.name`, `service.namespace=uno-arena`, immutable build/commit `service.version`, `deployment.environment.name`, pod UID as `service.instance.id` with hostname fallback outside Kubernetes, and bounded `unoarena.component` values such as `api`, `timer-worker`, or `room-runtime`. Keep `service.name` stable across API/worker roles and distinguish roles through `unoarena.component`. Player, room, game, tournament, command, event, and correlation identifiers are never resource attributes or metric labels; where permitted, they remain controlled span/log fields. Prometheus target metadata carries Kubernetes namespace, pod, and instance identity rather than copying all resource attributes onto every metric series.
- Put cross-service OpenTelemetry SDK, exporter, resource, propagation, bounded queue, shutdown, and `log/slog` JSON-handler mechanics in a separate infrastructure-only `platform/telemetry` Go module. The handler applies the shared resource identity and adds `trace_id`/`span_id` from the active context; logs still write one JSON object per line to stdout for Alloy collection, with no OTLP log exporter. Keep the existing `shared` module limited to its cross-context standard-library helpers. Every bounded-context service owns its instrument definitions, span names, attributes, post-commit recording seams, log event names/messages/domain fields, sanitization decisions, and log placement; the platform module contains no domain types, business metric catalog, or authoritative business behavior.
- Keep the telemetry module interface deep: `Start(ctx, config)` returns one runtime exposing the standard logger, explicit tracer/meter providers, and propagator, while `runtime.Shutdown(ctx)` performs bounded teardown. Start exactly once before constructing HTTP, Kafka, Redis, or Postgres adapters and pass providers/propagators explicitly. The one process-global provider exception is the same runtime MeterProvider, registered once at startup because OpenTelemetry's SDK self-observability and many instrumentation libraries use the standard global meter seam; no global TracerProvider or propagator is installed. Register the standard global OpenTelemetry error handler on the structured logger so exporter errors are sanitized. Production does not swap global providers during shutdown; singleton behavior is verified in isolated tests. `Start` validates required configuration and synchronously binds the metrics listener before returning. Disabled mode supplies no-op trace/metric providers but the same JSON logger. Exporter factories, registries, queues, and metrics-server details remain internal seams. On termination, stop accepting/processing business work first, then call telemetry shutdown for one bounded flush.
- Enable the pinned SDK's opt-in `OTEL_GO_X_OBSERVABILITY=true` only in required telemetry mode. Use its official nonblocking BatchSpanProcessor with maximum queue `2048`, maximum export batch `512`, schedule delay `5s`, export timeout `5s`, no `WithBlocking`, and OTLP retry bounded by the same export deadline. Queue capacity, size, processed count, and `queue_full` drops flow through the Prometheus MeterProvider. These experimental SDK self-metrics are internal operational signals, not stable business contracts; the pinned SDK version and offline exposition tests detect change. A small exporter wrapper emits a bounded local failure counter and sanitized structured error for OTLP export failures that the SDK queue metrics do not classify.
- Tempo uses a dedicated `tempo-traces` bucket on local MinIO, 24-hour trace retention, and a separate 1 GiB PVC for WAL/work data; both MinIO and Tempo pod replacements preserve accepted traces, while cluster deletion removes both PVCs. The single-node kind overlay suppresses startup compaction of its short-lived block set with a 1,000-block minimum input group, cuts live-store blocks every ten minutes, and refreshes the object-store block list every minute so a completed block becomes visible within bounded acceptance; production retains normal 2–4 block compaction, 30-second blocks, five-minute polling, and defaults to an external S3 bucket. The local Alloy-to-Tempo exporter keeps a bounded 2,048-request in-memory queue, uses two consumers, permits a 30-second request during node contention, and retries for at most ten minutes; application exporters remain independently bounded and nonblocking. Grafana provisions Tempo as its third data source and links traces to Loki through `trace_id` and `span_id` structured-log fields. Cover inbound/outbound HTTP, Kafka produce/consume, database operations, command handling, durable commit/publication, and background workers without attaching credentials, private gameplay state, or raw payloads.
- Local `kind` uses the custom `x_parentbased_mutations` head sampler at the trusted Gateway: accepted `POST`, `PUT`, `PATCH`, and `DELETE` roots are always sampled for deterministic command evidence, while `GET` and other autonomous roots use the production-like `0.001` ratio. Other long-running services use configurable `parentbased_traceidratio=0.001` (0.1 percent) for autonomous root work and all services follow parent decisions across HTTP and Kafka; short-lived repository-owned rebuild acceptance Pods may use always-on sampling for deterministic evidence. The Gateway establishes the root decision rather than honoring an untrusted external sampled flag. This keeps the best-of-three acceptance traffic within a single-node kind cluster's bounded telemetry capacity without weakening mutation-trace proof. The local Alloy gateway batches spans without tail sampling.
- The single-node kind overlay defers Tempo compaction measurement and scheduling for one hour so object-store maintenance stays outside the acceptance run, and sets the scheduler work-prune age to three hours to preserve Tempo's required two-measurement safety relation; trace blocks still write immediately to S3-compatible storage and retain the local 24-hour policy. The base deployment keeps one-minute measurement, a 30-second minimum cycle, and a one-hour work-prune age.
- The single-node kind profile runs repository-owned API health checks every 20 seconds, local Postgres and Redis exec readiness/liveness checks every 15/30 seconds, Debezium Connect checks every 20/30 seconds, and acceptance bots every five seconds. These bounded local cadences preserve health detection and the same authoritative public flows while preventing kubelet probe processes and polling reads from monopolizing the shared control-plane node during active log and trace ingestion. Base, staging, and production application probes retain their ten-second cadence, and the CLI default is unchanged.
- Empty-cluster bootstrap gives cold-cache Istio image pulls a ten-minute local install/readiness budget. The images remain exact multi-architecture pins and the timeout is overrideable; no warmed node or preloaded architecture-specific child image is required.
- Carry nullable W3C `traceparent` and `tracestate` atomically in all Kafka-bound transactional outboxes and Room's realtime outbox. Debezium Connect maps them to Kafka headers; Debezium Server maps them to Redis entry metadata; direct Kafka clients inject/extract the same headers. Consumers create spans from the extracted parent context. Do not persist W3C baggage through CDC paths.
- The canonical live distributed-trace proof is the accepted casual-game command that causes `GameCompleted`. One causal trace must cover the Gateway inbound HTTP span, Gateway-to-Room HTTP client/server spans, Room command and durable commit, Game Integrity append, Room transactional outbox insertion, Debezium-mediated Kafka publication, and Ranking consumer plus durable commit. Debezium transports the stored W3C context but need not emit its own span. Tempo must return the complete cross-service trace, and Loki must return correlated structured logs by both `trace_id` and the command's `correlationId` without promoting either to a label.
- Telemetry export is asynchronous and bounded. Batch queues, retries, exporter timeouts, and shutdown flushes have finite limits; saturation or an Alloy/Tempo outage drops spans and reports sanitized exporter/drop signals instead of blocking commands, HTTP responses, Kafka processing, or authoritative commits. A pre-outage batch may still be dropped through a stale gRPC connection after Alloy becomes Ready. Recovery acceptance therefore proves gameplay while Alloy has no pods, restores Alloy, gives the complete observability stack a bounded five-minute readiness window for the simultaneous exporter reconnect burst to drain on the single node, and then issues bounded fresh commands until one new complete trace proves exporter reconnection. Application readiness is independent of telemetry backends, while deployment acceptance still requires the full observability stack before telemetry evidence probes.
- Telemetry startup has explicit `disabled` and `required` modes. Direct/offline execution may opt into `disabled`; every Kubernetes Helm workload, including APIs and workers, must render `required`, and static acceptance rejects any disabled or unclassified workload. In `required` mode, invalid resource attributes, exporter settings, or metrics-listener binding fail process startup rather than silently degrading. After successful initialization, backend reachability remains outside application readiness and business-operation gates.
- Standardize telemetry configuration on `TELEMETRY_MODE`, existing `SERVICE_NAME` and `DEPLOYMENT_ENV`, immutable `SERVICE_VERSION`, bounded explicit `UNOARENA_COMPONENT`, Kubernetes Downward-API `POD_UID`, standard `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_EXPORTER_OTLP_PROTOCOL`, `OTEL_TRACES_SAMPLER`, and `OTEL_TRACES_SAMPLER_ARG`, plus `METRICS_ADDR=:9090`. Helm supplies all required values to every API and worker, including dynamically created Room runtime pods. Component identity is explicit rather than inferred from pod names or arbitrary role text.
- Compile the telemetry module and all services with repository language level `go 1.26.0` and toolchain `go1.26.5`. Pin OpenTelemetry core/trace/metric/SDK/SDK-metric and OTLP/gRPC exporter `v1.44.0`, Prometheus exporter `v0.66.0`, `otelhttp v0.69.0`, franz-go `kotel v1.7.0`, `redisotel/v9 v9.21.0`, `otelpgx v0.11.1`, and Prometheus Go client `v1.23.2`; align all repository `go-redis/v9` consumers on `v9.21.0`, retaining `pgx/v5 v5.10.0` and franz-go `v1.21.5`. Dependency `go` declarations are minimum language requirements, not target toolchains. Go module versions do not constrain third-party OCI architecture selection; repository images remain compiled for the detected target architecture while upstream workloads follow their independent multi-architecture image contract.
- Pin generic third-party workloads to exact releases and verified OCI multi-architecture index digests containing Linux AMD64 and ARM64 children. Workload references use the immutable index and let the node runtime select its child; generic manifests never embed or pre-stage an ARM64 child. This applies to Istio `1.30.2`, Alloy `v1.17.1`, Loki `3.7.3`, Tempo `3.0.2`, Grafana `13.1.0`, Prometheus `v3.13.1`, kube-state-metrics `v2.19.1`, MinIO `RELEASE.2025-04-22T22-12-26Z`, and MinIO client `RELEASE.2025-04-16T18-13-26Z`. Images without a suitable index use a reviewed architecture map; KurrentDB is the current exception. MinIO is local-only; production uses external S3.
- The mandatory business dashboard contains three context-owned OpenTelemetry counters: instrument `unoarena.players.created` in Identity exports as `unoarena_players_created_total` with unit `1` and help `Number of authoritative player creations committed by Identity.`; `unoarena.games.completed` in Room Gameplay exports as `unoarena_games_completed_total` with unit `1` and help `Number of authoritative game completion transitions committed by Room Gameplay.`; `unoarena.tournaments.completed` in Tournament Orchestration exports as `unoarena_tournaments_completed_total` with unit `1` and help `Number of authoritative tournament completion transitions committed by Tournament Orchestration.` Each process records an unlabeled zero once after telemetry startup so the descriptor and a genuine pre-flow baseline exist on an empty cluster; that initialization does not claim a business transition. Each instrument is injected at its context's post-commit seam and records one only after the corresponding new authoritative state transition commits; rejects, failed commits, and idempotent replays never increment it. These process-local counters are operational evidence of business activity, not authoritative accounting: a process failure in the commit-to-record window may undercount, and durable context state and events remain the source of truth. Dashboard totals use `sum(increase(<counter>[$__range]))` so replica aggregation and counter resets are handled over the selected interval. The instruments define no labels; bounded service, namespace, component, pod, and instance dimensions come only from Prometheus target discovery. Raw exposition contract tests reject instrument labels and pin the exact public series names, counter types, unit, and help text across OpenTelemetry SDK/exporter upgrades.
- A separate Kubernetes cluster dashboard covers workload readiness, unavailable replicas, pod restarts, and pod/container CPU, memory, network, and filesystem usage. Standalone Prometheus scrapes kube-state-metrics for Kubernetes object state and uses scoped authentication to scrape kubelet/cAdvisor for pod and container resources. Metrics Server is not a Prometheus history source, and local node-exporter is excluded because a kind node is itself a Docker container and its host-level panels would be misleading. Cluster telemetry is operational and must not be mixed into the business dashboard.
- Propagate `correlationId` across the BFF, services, workers, and consumers.
- Include `commandId`, `eventId`, `causationId`, `roomId`, `gameId`, `tournamentId`, and `schemaVersion` where applicable.
- Log command acceptance, rejection, append confirmation, and broadcast completion.
- Trace internal hops from envelope to durable append to downstream publication through the ADR-0047 OpenTelemetry/Alloy/Tempo path.
- Expose metrics for SSE connection counts, command latency, append latency, consumer lag, timer lag, and projection rebuild duration.
- Emit structured operational/security audit records for every rejected command with `commandId`, `correlationId`, session/player, room/tournament when known, rejection reason, submitted/current sequence when applicable, and timestamp. These records are not domain events and are not Game Integrity entries.
- Rejected-command audit writes remain context-owned and fail closed. For durable rejected commands, the context emits the audit before committing the idempotent rejected outcome: an audit failure therefore leaves no stored prior outcome and a retry re-attempts the audit. A crash after audit success but before the context commit may duplicate the operational record, so this seam is intentionally at-least-once; adapter deduplication compares stable rejection fields and excludes the emission timestamp. Their adapters construct a `slog.Record` and invoke the standardized handler's `Handle` method directly so JSON encoding or stdout write errors propagate to the command path; ordinary logger convenience methods are not used for this seam because they do not return handler failures. Required Kubernetes mode emits audits into the same stdout JSON stream collected by Alloy and forbids audit-file paths or volumes. Explicit offline/tests may retain file-backed sinks. Existing command-ID idempotency and conflicting-record checks remain inside each context adapter; the telemetry module owns only shared encoding/base-field mechanics.
- Track tournament provisioning batch success/failure, DLQ depth, round projection lag, and session invalidation close latency.
- Track Debezium connector availability, source WAL/replication-slot retention, connector offsets, capture lag, and oldest committed outbox event not yet visible in Kafka.
- Track the Room realtime Debezium Server Redis sink separately: replication-slot/WAL lag, Redis sink acknowledgments, offset durability, oldest undelivered player-feed entry, and duplicate/sequence-gap counts at gateways.
- Track each outbox's active/sealed partition sizes, seal high-water LSN, durable source offset, offset-to-high-water distance, safety-window age, reclaimable backlog, cleanup failures, and projected storage exhaustion.
- Track retries, DLQ publish failures, DLQ depth/oldest age, quarantined aggregate count/oldest age, replay attempts, and reconciliation outcomes per consumer group.
- Track idempotency-key conflicts, aggregate checkpoint gaps/regressions, owned-store commit failures, post-state/pre-offset duplicate deliveries, dedupe retention/cleanup lag, and cleanup blocked by replay/quarantine references.
- Track topic partition count against its immutable declaration, leader/replica distribution, per-partition throughput/size/lag/skew, topology epoch, cutover watermark, old-group drain, replacement buffer, and cutover quarantine.
- Track broker and failure-zone availability, ISR shrink, under-replicated/offline partitions, leader imbalance, replica reassignment/recovery, `acks=all` failures, idempotent-producer errors, and Connect internal-topic replication.
- Track bytes and time retained per topic class, oldest unconsumed/DLQ record, time-to-expiry, retention-policy drift, active replay/quarantine/rollback holds, snapshot/backfill activation, and checkpoint reconciliation outcome.
- Track each Postgres cluster's primary/standby health, sync state and flush lag, quorum loss, WAL growth, fenced promotions, writer-endpoint convergence, pool reconnect storms, unknown commit outcomes, and CDC offset/resume gaps.
- Track WAL/archive and backup age, upload/encryption failures, restore-point coverage, backup storage growth, historical key recoverability, last successful drill/evidence age, measured RPO/RTO, invariant/replay failures, and secure teardown completion.
- Track Room request distribution, native table-partition skew, hot rooms, database pool saturation/availability, and both CDC pipelines for the context database.
- Track Room row-lock wait and hold percentiles, lock/statement timeout counts, transaction duration, Game Integrity latency inside the lock, and reconciliation after append-confirmed/local-commit-failed cases.
- Track Room timer sorted-set and in-flight depth by bucket/type, oldest overdue score, claim batch latency, lease expiry/retry rate, definitively stale acknowledgments, and Postgres-to-Redis rebuild progress.
- Track authoritative Identity validation p50/p95/p99, Postgres partition/pool saturation, fail-closed timeout/error rate, stale-cache early rejections, superseded-session attempts, and validation-to-revocation race outcomes.
- Track Identity database pool/index saturation, validation lookup failures, database availability, and Debezium lag.
- Track OIDC discovery/JWKS refresh age/failures, provider latency/errors, issuer/audience/signature/nonce rejection counts, claim-mapping errors, Keycloak realm readiness, and production test-provisioning gate violations.
- Track Game Integrity encryption/decryption latency and failures, key-provider availability, active/historical key versions, rotation/rewrap progress, decrypt/export requests and denials, and commitment mismatches.
- Track istiod/CNI/ztunnel availability, certificate age/rotation failures, mTLS handshakes, denied connections, service-account identity mismatches, HBONE connection/throughput saturation, and policy rollout drift.

## Error Handling

- Stale room mutations should be rejected cleanly, not retried blindly.
- Identity/Postgres validation failure rejects state-changing commands; cached-positive sessions cannot be used as an availability fallback.
- Existing Room mutations acquire the aggregate row lock before sequence and rule validation. Lock timeout is an explicit transient failure, not permission to bypass serialization or append optimistically.
- Rejected commands emit structured operational/security audit records only; they never produce domain events or Game Integrity entries.
- Timer and reconnect handlers should be idempotent and decide Uno openness from persisted absolute UTC `expiresAt` plus opening room sequence.
- Timer workers treat Redis claims as at-least-once hints: only Room's locked Postgres transaction can decide whether a deadline is current, stale, duplicate, or applicable.
- Projection consumers should tolerate duplicate or out-of-order delivery where the upstream contract allows it.
- Replay or audit failures must not take down live gameplay.
- Encryption failure must fail a Game Integrity write before append; plaintext fallback is forbidden. Replay/audit key failure remains isolated from already-running gameplay unless the unavailable key is required for a live game operation.
- Saga fallback events should be emitted only when fallback changes business process state, such as quarantine, reconciliation, or recorded retry state.
- Infrastructure fallback such as broker redelivery, DLQ depth, worker restarts, and transient timeouts should be observable through metrics/traces unless it changes domain state.
- A consumer commits a poison source offset only after its consumer-owned DLQ acknowledges the original envelope and failure metadata. Ordered aggregates remain quarantined until replay/rebuild proves continuity.
- A successful consumer commits owned state, the contract idempotency key, and its sequence/version checkpoint before committing the Kafka offset. A duplicate is ignored only when immutable identity agrees; gaps or conflicts quarantine the aggregate.
- Outbox reclamation fails closed when a partition is not sealed, its WAL high-water LSN is missing, the matching source offset is absent/incomparable, the connector is unhealthy, or the safety window remains open. Age-only deletion is forbidden.

## Schema and Contract Evolution

- Every event should declare `schemaVersion`.
- Bounded contexts should evolve their own contracts independently.
- Kafka event names and SSE payloads should be versioned deliberately.
- Public analytics schemas may evolve separately from gameplay schemas.
- The standardized outbox router columns are infrastructure metadata; the JSON payload and AsyncAPI schema remain owned and versioned by the producing bounded context.

## Deployment and Networking

- Istio Ambient is required for production east-west L4 mTLS and workload identity, and the local `kind` acceptance lane installs the same security path. Enroll only intended namespaces and enforce strict mode plus explicit L4 authorization policies. Kind's plaintext exception is limited to documented application-layer database TLS and disposable credentials; it never disables mesh mTLS.
- Use per-node `ztunnel`; do not inject sidecars. Deploy L7 waypoint proxies only for an approved, concrete L7 routing or policy requirement.
- Retain application service credentials, authorization, correlation, Kubernetes NetworkPolicy, and least privilege; mesh identity complements rather than replaces them.
- Keep the BFF and stateful services close enough to reduce tail latency where practical.
- Deploy Keycloak only as the local/`kind` reference IdP. Production may use a managed standards-compliant provider configured through issuer/audience/JWKS settings without changing Identity's internal contract.
- Run Kafka Connect/Debezium privately, with one Kafka-bound PostgreSQL connector per context-owned database. Run one separate Debezium Server PostgreSQL-to-Redis pipeline for Room Gameplay's realtime outbox. Each replication identity is limited to logical replication plus its owned outbox table.
- Admission policy rejects in-place partition increases for ordered topics. A replacement topic, ACLs, connector routes, DLQ, dashboards, and consumer group must exist before a topology-epoch switch; old-topic deletion is a separate explicit retention action.
- Production admission requires at least three brokers, RF3, minimum ISR2, rack/zone placement, `acks=all`, idempotent producers, and disabled unclean election for every topic class. It rejects the RF1/min-ISR1 settings permitted in local Docker Compose and `kind`.
- Production topic policy applies the declared retention class and blocks deletion/reduction while consumer lag or replay/quarantine/rollback references would cross the new boundary. Local retention overrides are allowed only outside production.
- Run the outbox rotation/reclamation controller as an operational worker with database partition-maintenance rights and read-only access to the matching durable connector offset. It has no authority to mutate aggregate tables, fabricate offsets, or bypass the safety gate.
- Configure exactly one authoritative database endpoint per bounded context. Contexts own their schema definition, bootstrap lifecycle, connection pool, native table partitioning, and CDC configuration; no service receives another context's database credentials. Upgrade migrations require a later decision before the first stateful upgrade.
- The production endpoint represents one primary plus two synchronous standbys, requires primary-plus-one WAL-flush acknowledgment, and fences promotion to one writer. Runtime/CDC clients use promotion-aware writer discovery; stale standby endpoints are never used for authoritative decisions. Local single-instance endpoints are rejected as production HA configuration.
- Backup identities are separate from runtime/DDL roles and write encrypted material outside the primary cluster failure domain. Quarterly restore Jobs run only in isolated infrastructure with scoped decrypt access, record evidence, and securely destroy restored data after verification.
- Run one context-owned bootstrap Job before each Postgres-backed service and its CDC connector. The Job uses one admin advisory-locked transaction that mutates roles only after an empty-state decision, applies DDL through the dedicated bootstrap role, and grants runtime DML without DDL; exact/drift states receive zero mutations. Deployment fails on any schema version other than empty or exact-current (exactly one migrations row and one bootstrap-meta row), and never resets data implicitly.
- Run the Analytics-owned ClickHouse bootstrap Job before Analytics workloads. Runtime credentials have no DDL privilege and unexpected non-empty schema fails unchanged.
- Do not run schema Jobs for KurrentDB or Redis. Game Integrity validates deployment-provisioned KurrentDB policy and lazily creates expected-revision streams. Redis owners validate versioned key prefixes/capabilities and load scripts on every target node, with safe `NOSCRIPT` reload.

## OpenAPI and AsyncAPI Intent

- OpenAPI under `contracts/openapi/bff-v1.yaml` formalizes the BFF command envelope, snapshot routes, status/error response shape (including `409 snapshot_required`), SSE endpoints, and versioned resources.
- Command envelopes should use a discriminated schema where `type` maps to the design command catalog and `payload` is a `oneOf` by command type.
- AsyncAPI under `contracts/asyncapi/kafka-v1.yaml` formalizes topic names, event schemas (including Room `GameCompleted`/`MatchCompleted` bodies), producers, consumers, partition keys, idempotency keys, and schema-version policy. The Kafka envelope is authoritative; offline HTTP bridges are documented destination-specific transforms of the same event names and canonical fields. Durable Kafka adapters are implemented in consuming services when `KAFKA_BROKERS` is configured; clean kind CDC and recovery-worker lanes are live-proven.
- Scope service credentials per bounded context. Exact cross-chart credential equality and producer-scope requirements are documented in root `README.md` and Helm `values.yaml` comments (secret key names only; no hardcoded secret values).
- Helm Kubernetes Services and peer URLs use port 8080 consistently. Room timer and Tournament provisioning/seeding workers stay `enabled: false` outside kind until ops explicitly enable them. Spectator/Analytics projection-rebuilder workers implement ADR-0039 rebuild-request consumers plus context-owned snapshot/backfill callers; after live kind recovery proofs, only `values.kind.yaml` enables them. Default/staging/production remain false.
- Do not weaken `/ready` outside explicit capability mode: configured Gateway requires Redis rate-limit + LiveFeed clients (ping every configured Redis client); Room Postgres session and Game Integrity KurrentDB adapters intentionally keep the service not-ready until durable stores are reachable. Gateway Kafka SessionInvalidated consumption and Debezium player-feed decode are implemented. The clean ARM64 kind foundation, CDC, and all eight services have deployed successfully; this local proof makes no production capacity or HA claim.

## Testing Strategy

- Contract tests for command envelopes, snapshot schemas, and SSE payloads, including Uno `expiresAt` plus `openingSequence` and `409 snapshot_required`.
- Integration tests for room append-before-broadcast behavior.
- Rejection tests that assert structured operational/security audit records and the absence of domain events or Game Integrity appends.
- Replay tests for Game Integrity.
- Projection tests for Spectator View privacy filtering.
- Spectator admission tests for `waiting`/`locked`/`in_progress`, denial after `RoomCompleted`/`RoomCancelled`, and terminal stream closure.
- Host-leave tests for ad-hoc lowest-occupied-seat reassignment or immediate cancel before lock/start, and no host gameplay authority after lock/start.
- End-to-end tests for session invalidation closing live streams.
- Concurrency tests linearize command authorization against new-login/revocation commits: pre-revocation admissions may finish, while every post-commit validation rejects even with deliberately stale Redis/gateway cache entries.
- Identity persistence tests prove unique issuer/subject mapping, indexed token-hash lookup, single-active-session replacement, revocation ordering, database-outage fail-closed behavior, and no cross-context database access.
- OIDC tests use a real Keycloak realm locally to prove discovery/JWKS validation, issuer/audience/signature/expiry/nonce handling, subject mapping, signing-key rotation, provider outage behavior, and production rejection of test-only provisioning/direct-grant settings.
- Game Integrity security tests inspect KurrentDB records/backups for absent plaintext seed/private-card material, verify commitment/replay correctness, exercise KMS outage and key rotation, prove historical-key retention, and assert authorization plus durable audit for every decrypt/export.
- Mesh integration tests verify strict mTLS, expected SPIFFE/service-account identities, default-deny behavior, explicit allowed flows, plaintext rejection, certificate rotation, ztunnel disruption/recovery, and absence of sidecars. The `kind` lane verifies behavior, not target-scale throughput.
- This implementation drives those checks through the repo-owned simple CLI as the sole client/test interface; graphical UI testing is deferred and must still use only the BFF REST/SSE boundary.
- CLI countdown display for Uno windows is advisory; tests assert server `expiresAt`, `openingSequence`, SSE corrections, and command outcomes rather than local clock authority.
- Offline capability adapters (capability-mode memory session / in-memory limiters / GI memory / Analytics memory / HTTP bridge transforms) are tested explicitly; they must not be read as claiming durable Postgres/Kafka/Redis/KurrentDB adapters. Analytics durable ClickHouse HTTP/store and Analytics Kafka ingestion are implemented separately when configured.
- Production integration tests use real PostgreSQL logical replication, Kafka Connect, Debezium Outbox Event Router, and Kafka to prove commit-to-topic delivery, connector restart recovery, partition-key ordering, and duplicate-safe consumers.
- Realtime integration tests use real PostgreSQL logical replication, Debezium Server's Redis sink, and Redis Streams to prove commit-to-feed delivery, connector restart recovery, room stream routing, duplicate/sequence handling, and snapshot fallback after trimming.
- Outbox lifecycle tests seal time partitions under concurrent writers, record WAL high-water LSNs, prove cleanup remains blocked before matching durable offsets and safety windows, prove independent Room Kafka/Redis gates, and verify connector outage or missing/corrupt offset fails cleanup closed.
- Postgres bootstrap tests race Job retries to prove advisory-lock serialization, transactional empty-schema creation, exact-version no-op, rejection of partial/older/newer schemas without mutation, runtime-role DDL denial, and service readiness failure before successful bootstrap.
- Postgres HA tests lose a standby, lose the primary, and lose synchronous quorum to prove acknowledged commits survive, insufficient quorum fails writes, only one primary is writable, writer endpoints converge, pools recover without blind mutation retry, authoritative reads never hit stale replicas, and CDC resumes without skipping outbox records.
- Quarterly DR tests restore each Postgres context to a selected point and meet 5m/30m, restore KurrentDB with historical key access and meet 5m/60m, restore/backfill ClickHouse and meet 24h/4h, rebuild Redis from authoritative sources, verify schemas/invariants/commitments/checkpoints, and produce auditable teardown evidence.
- ClickHouse tests prove empty bootstrap, exact-version no-op, partial/older/newer rejection without mutation, and runtime-role DDL denial. KurrentDB tests prove lazy stream creation, event-ID idempotency, exact expected-revision conflicts, policy/readiness failure, encryption-key gating, native persistence on the selected AMD64 or ARM64 image, and restart recovery. Redis tests prove prefix/version validation, every-node script load, safe `NOSCRIPT` recovery, and rebuild after flush.
- Kafka failure-path tests inject transient and terminal consumer failures to prove bounded retries, DLQ-before-offset ordering, duplicate-safe DLQ publication, unrelated-key progress, aggregate quarantine, and successful replay/rebuild release.
- Consumer transaction tests crash after owned-store commit but before offset commit, replay exact duplicates, inject key/payload conflicts and sequence gaps, verify aggregate-only quarantine, and prove dedupe cleanup respects source retention plus the maximum replay/DLQ window and active references.
- Kafka topology tests assert environment-declared partition counts, reject in-place expansion, load-test the production 256/32 planning classes outside `kind`, and exercise versioned cutover in `kind` with reduced counts, new-topic buffering, durable producer watermark proof, old-group zero lag, checkpoint-ordered release, duplicate overlap, and aggregate-only gap quarantine.
- Kafka durability tests use at least three brokers to lose one broker/zone during produce and consume, verify acknowledged records survive, verify ISR-below-two fails publication, reject unclean election/config drift, and confirm domain, DLQ, replacement, and Connect internal topics retain RF3. The single-broker `kind` lane is excluded from HA assertions.
- Retention tests force each class across its boundary, verify Spectator rebuild-request → Room spectator-recovery-snapshot recovery and Analytics rebuild-request → context-owned analytics-backfill recovery, prove Identity remains authoritative after control expiry, reject checkpoint skips, retain DLQs/superseded topics while referenced, and validate local shortened windows make no production recovery claim.
- Rebuild-request tests prove keyed idempotency, bounded page/cursor limits (producer default 100 / max 1000; Analytics worker default page 1000), worker-owned DLQ-before-offset ordering, Spectator Redis CAS/fenced generation swap with atomic idempotency/quarantine release against concurrent live apply, Analytics durable ClickHouse rebuild generation/lease dual-write fencing and server-side clone across worker pods, held post-gap quarantine replay without skipping gaps, and quarantine release only after continuity is proven. Both live kind proofs passed, so kind enables the rebuilder Deployments; default, staging, and production remain disabled pending environment-specific operational enablement.
- Room persistence tests prove all aggregate state and both outboxes use the Room context database, native partition pruning/index use where configured, transaction atomicity, and database-outage fail-closed behavior.
- Room concurrency tests race turn, jump-in, reconnect, and timer commands against one aggregate to prove one integrity append/commit order, stale rejection before append, bounded lock timeout, Game Integrity rollback, and append-confirmed commit-failure reconciliation.
- Timer integration tests use real Redis Lua claims and Postgres deadlines to prove bounded batches, exclusive leases, worker-crash reaping, duplicate/stale safety, bucket isolation, Redis flush rebuild, and overdue recovery.
