test:gateway:
  stage: test
  image: golang:1.26.5-alpine3.24
  script:
    - cd services/gateway/src && go vet ./... && go test ./...
  rules: &gateway_validation_rules
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main"'
      changes:
        - services/gateway/**
        - shared/**
        - ci/templates/**
        - .dockerignore
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
      changes:
        - services/gateway/**
        - shared/**
        - ci/templates/**
        - .dockerignore
    - if: '$CI_PIPELINE_SOURCE == "web" && $CI_COMMIT_BRANCH == "main" && ($RUN_SERVICE == null || $RUN_SERVICE == "" || $RUN_SERVICE == "all" || $RUN_SERVICE == "gateway")'

build:gateway:
  stage: build
  needs: ["test:gateway"]
  script:
    - docker build -f services/gateway/Dockerfile -t $CI_REGISTRY_IMAGE/gateway:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA .
  rules: *gateway_validation_rules

deliver:gateway:
  stage: deliver
  needs: ["build:gateway"]
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker push $CI_REGISTRY_IMAGE/gateway:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
    - |
      CHART_VERSION=0.1.$CI_PIPELINE_IID
      helm package services/gateway/helm/gateway --version $CHART_VERSION --app-version $CI_COMMIT_SHORT_SHA
      curl --fail-with-body --request POST \
        --user gitlab-ci-token:$CI_JOB_TOKEN \
        --form "chart=@gateway-$CHART_VERSION.tgz" \
        "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/helm/api/${CI_COMMIT_REF_SLUG}/charts"
  rules: &gateway_main_push_rules
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
      changes:
        - services/gateway/**
        - shared/**
        - ci/templates/**
        - .dockerignore
    - if: '$CI_PIPELINE_SOURCE == "web" && $CI_COMMIT_BRANCH == "main" && ($RUN_SERVICE == null || $RUN_SERVICE == "" || $RUN_SERVICE == "all" || $RUN_SERVICE == "gateway")'

deploy-staging:gateway:
  stage: deploy-staging
  needs: ["deliver:gateway"]
  when: manual
  script:
    - echo "deploy-staging for gateway is stubbed for this checkpoint"
  rules: *gateway_main_push_rules
