{{- /*
  Provisioning worker remains disabled: no safe WORKER_ROLE=tournament-provisioning
  loop is implemented. Synchronous ProcessProvisioningBatch + durable recovery cover
  the application path. Keep provisioningWorker.enabled=false in all overlays.
*/ -}}
{{- if .Values.provisioningWorker.enabled }}
{{- fail "provisioningWorker.enabled must stay false until a safe WORKER_ROLE loop exists" }}
{{- end }}
