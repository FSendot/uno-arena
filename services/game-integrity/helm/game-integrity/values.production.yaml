replicaCount: 2

env:
  EVENTSTORE_URL: esdb://eventstore.uno-arena.svc.cluster.local:2113?tls=false
  KAFKA_BROKERS: kafka.uno-arena.svc.cluster.local:9092
  # Production rollout blocked: EventStoreDB adapter absent → /ready eventstore_adapter_blocked.

existingSecret: game-integrity-secrets

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi
