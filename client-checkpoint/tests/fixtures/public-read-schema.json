{
  "description": "Schema-only expectations for public ranking/analytics reads on a fresh local deployment. Arrays may be empty; do not require populated async projections.",
  "leaderboard": {
    "requiredKeys": ["entries"],
    "entriesType": "array"
  },
  "analytics": {
    "requiredKeys": ["authoritative", "gameplayMetrics", "tournamentStats", "ratingStats"],
    "authoritative": false,
    "arrayKeys": ["gameplayMetrics", "tournamentStats", "ratingStats"]
  }
}
